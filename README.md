Client Certificate Proxy Example
================================

> This is an example of SSL Termination Proxy that can handle client certificates.

## Usage

```
$ docker-compose up
```

ブラウザにて `https://localhost` にアクセス
